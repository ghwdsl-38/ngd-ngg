# NGD-NGG本地项目整理说明

本目录从以下当前工作区整理而来：

```text
/mnt/data0/volcano-scheduler/ngd-ngg-scheduling-demo
```

整理目标是保留可部署、可阅读、可继续开发和可重新测试的文件，同时不携带镜像及历史运行产物。

## 保留内容

| 目录或文件 | 内容 |
|---|---|
| `prc/` | PRC Go源码、controller-runtime Controller及单元测试 |
| `algorithm_server/` | Go Algorithm Server、Python Worker和1000节点演示代码 |
| `topology_agent/` | LLDP/Bond采集、Node打标源码及单元测试 |
| `deploy/` | 自动生成并部署Kubernetes/Compose配置的入口 |
| `deploy-manual/` | 可直接使用`kubectl apply -k`的手工部署清单 |
| `config/` | 通用组件清单、RBAC和上层拓扑样例 |
| `docs/` | 当前方案、需求方新版CRD/接口及阶段文档 |
| `go_test_suites/` | 当前Go测试、envtest、Mock Prometheus、测试输入和Expected |
| `manifests/monitoring/` | Prometheus、node-exporter和kube-state-metrics清单 |
| `image_validation/` | 从Docker Hub拉取镜像后的验证脚本，不包含旧结果 |
| `scripts/`、`Makefile` | 测试、构建和部署辅助入口 |
| 根目录Dockerfile和Go Workspace | 源码构建定义及多模块工作区 |

## 未复制内容

- `.git/`：原仓库历史；
- `.cache/`：Go依赖、Go构建和envtest缓存；
- `images/`：OCI/TAR镜像产物，部署时直接使用Docker Hub镜像；
- `build/`和`debug.test*`：本机编译及调试二进制；
- 所有`results/`、`runs/`：历史测试和运行输出；
- `deploy/generated/`：可由部署工具重新生成；
- `__pycache__/`、`*.pyc`和`.DS_Store`：本机临时文件；
- `test_suites/`：已被`go_test_suites/`替代的旧Python测试入口；
- `ngg_consumer/`：当前PRC、Algorithm和Topology Agent均未引用的历史辅助库；
- 旧归档`*.tar`及根目录的方案副本文档。

未复制真实Kubeconfig、Token或其他Secret。

## 本地开始使用

```bash
cd /mnt/data0/ngd-ngg
```

阅读整体说明：

```text
README.md
docs/阶段汇报-0907/NGD-NGG两层调度项目整体说明v1.0.md
```

使用Docker Hub镜像进行集群内显式Kubeconfig部署：

```text
deploy/README-集群内显式Kubeconfig部署.md
```

自动部署入口需要先生成本地配置：

```bash
python3 deploy/deploy.py init
```

随后编辑`deploy/config.local.json`中的集群Context、镜像地址、拓扑文件、Prometheus地址及认证配置。该文件和真实Secret不会进入版本库。

手工部署入口：

```text
deploy-manual/README.md
```

其中PRC和Algorithm已使用Docker Hub版本；LLDP清单仍包含`REPLACE_WITH_LLDP_IMAGE`，部署前必须替换为实际发布的LLDP镜像。

## 测试环境说明

源码和测试输入已保留，但Go依赖与envtest二进制缓存没有复制。首次在新机器运行测试时，需要按`go_test_suites/README.md`准备Go和envtest。`scripts/go-test-env.sh`已改为使用当前项目目录下的`.cache/`，不再依赖原始工作区绝对路径。
